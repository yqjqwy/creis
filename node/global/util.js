let util = require(util);

//util.inherits --- 原型继承
//util.inspect --- 将任意对象转换为字符串
//util.isArray(object) --- 传入的对象是数组返回 true  否则返回false
//util.isRegExp(object) --- 传入的对象是正则表达式返回 true  否则返回false
//util.isDate(object) --- 传入的对象是一个日期返回 true  否则返回false
//util.isError(object) --- 传入的对象是一个错误对象返回 true  否则返回false